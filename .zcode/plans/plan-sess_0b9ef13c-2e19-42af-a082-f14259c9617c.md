# Full overhaul: code, retraining, new experiments, paper rewrite — resolving all 20 review points

Decisions: rename "Hazard"→"Failure-Risk" AND add a true discrete-time hazard model (point 3 answered both ways); validity-focused retitle; every number in the paper regenerated from one consistent, leakage-clean pipeline.

## Workstream A — Pipeline correctness & tooling (before any reruns)
1. `src/benchmark_cv.py`: fix `run_cross_chem_per_cell` to train ONCE on the source (docstring already claims this; code retrains per test cell).
2. **Leakage-clean calibration (point 5)**: cross-fitted calibration — inner GroupKFold over outer-train cells produces out-of-fold scores; Platt/isotonic/temperature fit on OOF scores, applied to untouched test cells. Applies to within-dataset (trees + GRU) and to transfer (transfer model trains on all source cells; calibrator fits on source-OOF scores). Same change in `gru_cv.py`.
3. `src/stats_utils.py`: add cell-level bootstrap AUC CIs (resample cells), paired bootstrap for AUC deltas (primary test for the SOH ablation), ECE, log loss, calibration slope/intercept, PR-AUC, sensitivity@fixed FPR, net-benefit/cost metric `C(τ)=C_FN·FN+C_FP·FP`. DeLong stays, demoted to secondary.
4. `src/composite_label.py`: endpoint parameter `combined | soh_only | volt_only`; loaders regenerate labels per endpoint (point 14). Verify Severson loader exposes voltage; if not, volt-only labels run on NASA/CALCE/Oxford only.
5. GRU multi-seed (42, 1, 7); leave-one-cell-out CV for CALCE (7 folds) alongside NASA 5-fold GroupKFold.
6. New scripts (same resumable CSV style): `baselines.py`, `feature_ablation.py`, `faildef_ablation.py`, `same_chem_transfer.py`, `hazard_model.py`, `operational_metrics.py`, `monotonicity_check.py`.

## Workstream B — Experiments (all with cell-level CIs)
- **E1 Within-dataset rerun** (pts 4,5,6,17): trees + GRU, NASA GroupKFold + CALCE LOCO, new calibration; AUC, Brier, ECE, log loss, slope/intercept, PR-AUC, sens@FPR per horizon.
- **E2 True discrete-time hazard model** (pt 3): pooled per-offset hazards h_ℓ(t)=P(T_f=t+ℓ | T_f>t+ℓ−1, x_t) — one XGBoost + one logistic on the offset-expanded at-risk dataset (offset ℓ as a feature; operational: uses only x_t), cumulative risk R(t,H)=1−∏(1−ĥ_ℓ). Monotone in H by construction. Evaluate within-dataset + LCO→LFP transfer, compare against fixed-horizon classifiers; also report empirical monotonicity-violation rates of the fixed-horizon classifiers (pts 3,19).
- **E3 Minimal baselines** (pt 12): logistic SOH-only; distance-to-threshold (SOH−0.8) score & logistic; cycle-only; SOH+cycle; sensors-only; full logistic. Within + cross-chem. Expected: SOH-only reproduces most transfer AUC → centerpiece of the validity story.
- **E4 Factorial feature ablation** (pt 13): all 7 combos of {SOH, cycle, sensors} × 3 sources × Oxford/Severson at H=20 (XGBoost primary; existing ±SOH rows for other trees retained).
- **E5 Failure-definition ablation** (pt 1,14): retrain with soh_only / volt_only / combined labels; transfer at H=20 with and without SOH. Directly measures circularity: with-SOH transfer under a label SOH doesn't define.
- **E6 Same-chemistry controls** (pt 9): NASA↔CALCE (LCO↔LCO) and Severson→Oxford (LFP→LFP), with/without SOH, plus common-feature variants (also addresses pt 11 zero-imputation identifier risk). Isolates dataset-shift vs chemistry-shift.
- **E7 Fair architecture comparison** (pt 10): all four models on the identical common feature set {cycle, avg_voltage, min_voltage, SOH} for within + transfer.
- **E8 Recalibration study**: keep Arm A/B results (independent of within-LCO calibration), add cell-level CIs, note Oxford k∈{1,2} and 4/5-cell caveat.
- **E9 Operational metrics** (pt 18): cost curves at C_FN:C_FP ∈ {5:1, 20:1} + net-benefit for raw vs calibrated scores under shift.

## Workstream C — Tables & figures
- Update all existing tables with new numbers + per-cell ±std + bootstrap CIs; DeLong table gains a paired-bootstrap ΔAUC [CI] column.
- New: baselines table, feature-ablation table, failure-def table, same-chem control table, hazard-vs-fixed comparison, calibration-quality table (ECE/log loss/slope), operational-cost table.
- New figures: **"Transferability Collapse Map"** (ΔAUC heatmap, the reviewer's flagship figure), reliability diagrams from the new protocol, PR-AUC vs horizon, net-benefit curve. SHAP figures and deployment figures unchanged. Generate via extended plotting scripts into `paper_ieee_access/figs/` (single-panel discipline per AGENTS.md).

## Workstream D — Paper rewrite (`paper_ieee_access/main_access.tex`, edited directly; texflow MCP not attached — local pdflatex compile)
- **Retitle**: "Multi-Horizon Failure-Risk Models for Battery Failure Prediction: How Much Cross-Chemistry Transfer Is a State-of-Health Shortcut?"; update \markboth, keywords; rename terminology body-wide.
- Reframed abstract + intro around the validity/shortcut question; new contributions list (hazard model + validity analysis + controls + deployment).
- Methodology: full split-protocol documentation (GroupKFold/LOCO, cell-disjoint train/calib/test, cross-fitted calibration), hazard-model formulation section, endpoint definitions, honest Oxford description (46–78 cycles, ~100-cycle spacing, labels identical across H → pooled detection) and per-cell stats effectively 4/5 cells.
- Results: insert new subsections (baselines, ablations, failure-def, same-chem, hazard model, operational metrics) with new tables/figures; all numbers updated.
- Discussion/Conclusion: supported claims only — "much of the observed cross-dataset discrimination is attributable to SOH…", chemistry vs dataset shift resolved empirically by E6; DeLong demoted with pseudoreplication caveat; bootstrap CIs primary; expanded future work only for what remains undone.

## Execution order & verification
0. TodoWrite tracking; each experiment writes resumable CSVs; heavy runs in background shells in parallel.
1. A → smoke-test on one config → E1 → E3/E4/E6/E7 parallel → E2 → E5 → E8/E9 analyses.
2. Leakage audit script (assert cell disjointness across train/calib/test), determinism spot-check.
3. Regenerate figures/tables → rewrite tex → compile loop: `TEXINPUTS` pdflatex ×3, log grep, `pdftoppm -r 150` + PIL max-ink-x overflow scan (fail > 0.94) per AGENTS.md, `pdftotext` phrase checks; iterate until clean.
4. Delivery gate: render final PDF pages to PNG and dispatch the judge agent on the float-heavy + rewritten pages; fix verdicts.
5. Final summary mapping each of the 20 review points → its resolution.

Point map: 1→E5+B-text; 2,19→retitle+reframe; 3→E2+rename+monotonicity; 4→LOCO+docs; 5→cross-fitted calibration; 6→ECE/log-loss/slope+rewording; 7→paired cell bootstrap primary; 8→Oxford correction+caveat; 9→E6; 10→E7; 11→common-feature variants; 12→E3; 13→E4; 14→E5; 15→CIs everywhere; 16→collapse map; 17→PR-AUC; 18→E9; 20→rewritten conclusion.